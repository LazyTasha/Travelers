<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  	
<mapper namespace="db">
	<insert id="insertMember" parameterType="db.UserDataBean">
		insert into gg_user values(#{user_id}, #{passwd}, #{user_name},#{gender},#{email, jdbcType=VARCHAR}, #{reg_date})
	</insert>
	
	<select id="checkId" parameterType="String" resultType="int">
		select count(*) from gg_user where user_id=#{user_id}
	</select>
	
	<delete id="deleteMember" parameterType="String">
		delete from gg_user where user_id=#{user_id}
	</delete>
	
	<update id="modifyMember" parameterType="db.UserDataBean">
		update gg_user set passwd=#{passwd}, user_name=#{user_name} where user_id=#{user_id}
	</update>
	
	<select id="getMember" parameterType="String" resultType="db.UserDataBean">
	 		select * from gg_user where user_id=#{user_id}
	 </select>
	 
	 <select id="idcheck" parameterType="String" resultType="Integer">
     	select count(*) from gg_user where user_id=#{user_id}
	</select>
	
	<select id="checkname" parameterType="String" resultType="Integer">
     	select count(*) from gg_user where user_name=#{user_name}
	</select>
	 	 
</mapper>